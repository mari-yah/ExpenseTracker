{
  "name": "household-ledger",
  "version": "1.0.0",
  "private": true,
  "description": "Personal income & expense tracker with per-user accounts, deployed on Netlify with Netlify Blobs as the backend.",
  "scripts": {
    "build": "node build.js",
    "dev": "netlify dev",
    "test": "node test/test-function.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.2"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
